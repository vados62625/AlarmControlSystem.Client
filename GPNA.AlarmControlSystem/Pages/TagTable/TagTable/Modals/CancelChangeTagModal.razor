@using GPNA.AlarmControlSystem.Shared.ButtonsModal

<div class="modal-backdrop modal-backdrop-level-2 show"></div>
<div class="tag-table__modal-bg cancel-change-tag">
	<div class="d-flex align-items-center gap-3">
		<svg style="width: 15px; height: 14px; margin-bottom: 3px;">
			<use href="/img/icons/icons.svg#tag-modal-attention"></use>
		</svg>
		<p class="tag-table__modal-title">ОТМЕНИТЬ ИЗМЕНЕНИЯ</p>
	</div>
	<div class="w-100 h-100 p-0 d-flex flex-column gap-2 justify-content-between">
		<p class="modal-delete-text mt-2">Все внесенные вами изменения будут удалены.<br />Вы уверены, что хотите отменить изменения?</p>			

		<div class="tag-table__modal-button-container">
			<CancelButton ClickEvent="ConfirmAndClose" TypeModal="cancel-change-tag"/>
			<ReturnButton ClickEvent="Cancel" />
		</div>
	</div>
</div>

@code {
	[CascadingParameter] BlazoredModalInstance ModalInstance { get; set; } = default!;

	void Cancel()
	{
		ModalInstance.CancelAsync();
	}
	
	private async Task ConfirmAndClose() 
	{
		await ModalInstance.CloseAsync(ModalResult.Ok());
	}

}
